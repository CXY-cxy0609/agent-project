# 中小学/大学/教师场景企业级 Agent 架构升级方案（禁用 LangGraph 版）

## 1. 目标与范围

### 1.1 产品目标（已收敛）
- 从“考研问答”升级为“分学科个性化辅导平台”。
- 明确服务人群：**中小学生、大学生、教师**。
- 不覆盖人群：社会学习者、家长、职业培训/企业培训用户。
- 核心能力双主线：
  - 个性化辅导（按学段、学科、能力画像适配）。
  - 个性化教学视频生成（按知识点与学生画像生成讲解视频）。

### 1.2 技术约束（关键前提）
- Agent 编排**不使用 LangGraph**。
- 采用并强化现有自研 Harness，升级为企业级可治理编排框架。
- **不考虑迁移成本**，以目标态最优架构为优先。
- 本文仅输出技术方案，不直接改代码。

---

## 2. 当前架构盘点（As-Is）

## 2.1 项目结构（排除 `apps/manim-project`）
- `apps/web`：Vue3 前端，已具备学科、会话、知识库、学情页面结构。
- `apps/server`：NestJS 壳层，当前业务能力基本未落地。
- `apps/agent`：TypeScript + Express + 自研 Harness，当前最核心能力承载层。
- `apps/rag`：Python FastAPI + Qdrant，负责解析、索引、检索、向量记忆。
- `packages/shared`：共享类型，存在字段语义仍偏考研场景。

## 2.2 核心问题

### A. 业务边界不清
- Web 已按完整产品设计 API，但 Server 仍接近空壳。
- Agent 同时承担“编排 + 业务 API + 部分后端职责”，后续会成为维护瓶颈。

### B. 契约不统一
- SSE 事件协议与前端预期不一致。
- `subjectId` 的 string/number 语义混用，跨服务一致性不足。

### C. 场景硬编码
- Prompt/规则大量“考研限定”文案，不适配中小学与教师场景。
- 检索预处理对学科识别词典过窄，难以支撑 K12 + 大学多学科。

### D. Harness 企业化不足
- 有基础执行图与节点机制，但缺少企业级治理能力：
  - 版本化流程定义与灰度发布。
  - 可回放调试与断点恢复。
  - 统一策略注入与安全审计。
  - 工作流 SLA、超时预算、失败补偿。

---

## 3. 架构原则（To-Be）

- **Harness First**：自研编排框架作为核心资产，不依赖黑盒编排库。
- **业务域与 Agent 域解耦**：业务 API 与 Agent Runtime 分层治理。
- **人群收敛驱动模型设计**：只围绕中小学/大学/教师三类角色建模。
- **教学能力优先**：文本辅导与视频生成共用知识底座与策略体系。
- **可观测可治理**：每次推理都可审计、可追踪、可复现。
- **子流程原生化**：子流程必须采用调度器内建 SubGraph，而不是节点内部再调用 Agent/Graph。
- **通信解耦化**：节点通信采用“消息驱动 + 状态分区”，避免全局共享状态耦合。

---

## 4. 编排技术路线对比（重点：不用 LangGraph）

## 4.1 候选路线

### 路线 A：维持当前 Harness，小步补丁式优化
- 优点：改动小、交付快。
- 缺点：不符合“重构到企业级目标态”的要求，治理能力上限低。

### 路线 B：引入 LangGraph/同类框架
- 优点：初期搭建快，社区样例多。
- 缺点：你已明确指出的黑盒问题在企业治理中是硬伤，不符合要求。
- 结论：**排除**。

### 路线 C（推荐）：自研 Enterprise Harness + 工作流引擎托底
- 核心思想：业务编排语义由自研 Harness 定义；底层执行/重试/定时/补偿可借助 Temporal 等通用工作流引擎。
- 优点：
  - 编排语义可控、透明、可解释。
  - 企业级稳定性能力（重试/恢复/任务编排）可复用成熟底座。
  - 与高并发架构兼容（编排面与流量面天然分离）。
- 缺点：平台建设复杂度高，但在本项目中可接受（不受迁移成本约束）。

## 4.2 对比矩阵

| 维度 | 路线 A 补丁式 Harness | 路线 B LangGraph | 路线 C Enterprise Harness（推荐） |
|---|---|---|---|
| 满足“禁用 LangGraph”约束 | 是 | 否 | 是 |
| 可解释性/可控性 | 中 | 低-中 | 高 |
| 企业级治理能力上限 | 中 | 中 | 高 |
| 高并发架构适配性 | 中 | 中 | 高 |
| 初期投入 | 低 | 中 | 高 |
| 中长期维护成本 | 中-高 | 中 | 低 |

---

## 5. 推荐总体架构（教育人群专版，目标态）

## 5.1 分层架构

1. **体验层**
   - `apps/web`（学生端/教师端统一前端，按角色路由和权限展示）。

2. **业务域层（`apps/server` 作为核心业务后端）**
   - 身份与组织域：学生、教师、学校/班级。
   - 学科课程域：学段-学科-章节-知识点图谱。
   - 学习会话域：会话、消息、作业、测评记录。
   - 内容资源域：教材、讲义、题目、视频素材。

3. **Agent 平台层（以 `apps/agent` 升级）**
   - Enterprise Harness Runtime（自研编排内核）。
   - Model Gateway（模型路由、降级、成本控制）。
   - Tool Platform（权限、配额、版本与审计）。
   - Policy Guard（学段策略、内容安全、教学规范）。

4. **RAG 与知识层（`apps/rag`）**
   - 向量检索 + 关键词检索 + 元数据过滤。
   - 知识引用与证据回传（回答可溯源）。

5. **平台基础层**
   - PostgreSQL + Redis + Kafka（或同级 MQ）+ OpenTelemetry 观测。

## 5.2 技术栈建议（目标态定版）

- **前端**：Vue3（保持不变）。
- **业务后端（Server）**：**Go + Gin（或 Fiber）+ gRPC（内部）+ REST（外部）**。
  - 选型理由：高并发连接处理能力强、运行时开销低、易做水平扩展，适合作为长期业务中台与 API 网关核心。
- **Agent 平台**：TypeScript 自研 Enterprise Harness（不依赖 LangGraph）。
  - 保留 TS 有利于模型 SDK、工具生态和 Prompt 工程效率。
- **RAG 服务**：Python FastAPI + Qdrant + OpenSearch（混合检索）。
- **工作流底座**：Temporal（必选）用于长流程、补偿、失败恢复、任务编排。
- **数据与基础设施**：PostgreSQL + Redis + Kafka + OpenTelemetry + Prometheus + Grafana。

## 5.3 为什么业务后端定为 Go（结论性说明）

- 在“不考虑迁移成本”的前提下，业务后端应直接选择长期并发上限更高的栈。
- 业务后端承担认证、会话、资源管理、任务编排入口、实时连接网关等高流量职责，Go 更匹配该职责。
- AI 能力层继续由 TS/Python 各司其职，形成“Go 扛流量、TS 扛编排、Python 扛检索”的分层架构。

---

## 6. Enterprise Harness 升级蓝图（核心）

## 6.1 从“可跑”到“可治理”的能力清单

1. **编排定义层**
   - Workflow DSL（JSON/YAML + TS Schema）版本化。
   - 节点类型标准化：LLM、Tool、Judge、Branch、Parallel、HumanReview、Finalize。

2. **执行引擎层**
   - 统一状态机（运行中/成功/失败/补偿中/取消）。
   - 幂等执行键（防重复调用）。
   - 节点级超时、重试策略、退避策略。

3. **上下文与记忆层**
   - Session Memory、Learner Profile Memory、Teacher Context Memory 分层。
   - 输入输出结构化（严格 Schema 校验）。

4. **可观测层**
   - 每个节点的 trace/span、token、耗时、成本。
   - Workflow Replay（按 traceId 回放当时执行）。
   - Prompt 与模型版本关联审计。

5. **治理层**
   - Policy Injection：按学段/学科/角色注入策略。
   - 安全网关：违规内容拦截、敏感问题升级人工。
   - 灰度发布：工作流版本按租户/班级/用户桶灰度。

## 6.2 推荐模块拆分（满足工程化）

- `apps/agent/src/harness/core`：状态机、执行器、checkpoint、workflow-control。
- `apps/agent/src/harness/runtime`：workflow runtime、scheduler、retry policy。
- `apps/agent/src/harness/governance`：policy、audit、guardrails。
- `apps/agent/src/harness/observability`：tracing、metrics、event sink。
- `apps/agent/src/harness/sdk`：供业务域调用的稳定接口层。

> 说明：以上拆分会自然满足你“单文件不超过 500 行”的工程约束。

## 6.3 调度与通信规范（新增强制约束）

### A. 子流程必须是调度器原生 SubGraph
- 每个复杂能力域（如 QA、Video、Repair）在调度层定义为独立 SubGraph。
- SubGraph 由调度器统一生命周期管理（启动、暂停、重试、补偿、取消）。
- 禁止在节点代码内直接 new Agent 或嵌套执行 Graph，防止绕开统一治理。
- 统一收益：全链路可观测、统一超时预算、统一重试策略、统一回放。

### B. 节点通信改为“消息驱动 + 状态分区”
- 节点之间仅通过标准事件消息交换，不直接读写其他节点私有状态。
- 状态存储拆分为：
  - `workflow_state`：流程级共享最小状态（只放必要路由信息）。
  - `node_state/<node_id>`：节点私有状态分区（不可被其他节点直接修改）。
  - `artifact_state`：大对象状态（截图、代码、渲染日志、视频元数据）。
- 事件总线消息建议结构：`event_id`, `workflow_id`, `subgraph_id`, `node_id`, `event_type`, `payload`, `timestamp`, `trace_id`。
- 统一收益：降低共享状态冲突，提升并发执行安全性和可追溯性。

---

## 7. 面向三类用户的人群模型重构

## 7.1 角色体系（收敛版）
- `StudentK12`（中小学生）
- `StudentCollege`（大学生）
- `Teacher`
- `Admin`（平台运营/教研管理员）

## 7.2 个性化策略关键字段
- `education_stage`：primary / middle / high / college
- `subject_code`：学科编码（统一枚举）
- `learning_goal`：预习/巩固/冲刺/讲题/备课
- `cognitive_level`：基础/中等/拔高
- `explanation_style`：图解型/步骤型/启发型

## 7.3 教师专属能力
- 生成课堂讲解稿与板书提纲。
- 基于班级薄弱点自动生成分层练习与讲解视频脚本。
- 提供“可编辑草稿”而非不可控的一次性成品。

---

## 8. 个性化辅导与个性化视频的双引擎设计

## 8.1 辅导引擎（Tutoring Engine）
- 输入：题目/知识点 + 学生画像 + 历史错误模式。
- 输出：分层解释、步骤化解题、即时小测。
- 约束：中小学输出语言更浅显；大学阶段增加严谨推导。

## 8.2 视频引擎（Teaching Video Engine）
- 输入：知识点目标、受众学段、时长、风格模板。
- 中间层：分镜规划 -> 讲解脚本 -> 视觉脚本 -> 渲染任务。
- 输出：视频 URL + 章节索引 + 可编辑脚本。
- 关键要求：视频内容与文字辅导共用同一知识证据源，避免口径不一致。

## 8.3 已生成视频的截图诊断与自动修复闭环（新增）

### A. 业务能力定义
- 用户在会话中上传视频截图（可多张），指出“这段有问题”。
- 系统自动定位问题来源（脚本逻辑/动画对象/公式排版/镜头时序/旁白文案）。
- 生成最小代码修复补丁，触发重渲染，并回传修复前后对比结果。

### B. 推荐流程（作为 `VideoRepairSubGraph`）
1. `collect_evidence`：接收截图、用户描述、原视频 ID、原 Manim 脚本版本。
2. `visual_diagnose`：多模态模型分析截图异常点，输出结构化诊断。
3. `map_to_code`：基于渲染日志与代码符号索引，将视觉异常映射到具体代码段/场景段。
4. `patch_proposal`：生成最小可行补丁（优先局部修复，避免整段重写）。
5. `static_validate`：语法检查、对象存在性检查、时长约束检查。
6. `render_retry`：渲染修复版本，失败时进入受控重试（最多 N 次）。
7. `visual_regression_check`：对比修复后关键帧，判断问题是否消除且无新增明显回归。
8. `publish_result`：产出新视频版本、变更摘要、可回滚版本号。

### C. 为什么这么设计
- 截图是用户反馈最自然方式，能降低表达成本并提高定位效率。
- 视觉异常与代码异常不是一一对应，必须通过“视觉诊断 + 代码映射”双阶段保证准确率。
- 用 SubGraph 承载修复流程，能与主视频流程共享治理能力（重试、回放、审计、灰度）。
- “最小补丁 + 视觉回归检测”可降低修复引入新问题的概率。

### D. 关键实现要点
- 为每次视频生成保存 `script_version`, `scene_manifest`, `render_log`, `frame_index`。
- 建立“帧时间轴 -> 场景对象 -> 代码符号”映射索引，支持截图快速反查。
- 修复任务必须输出 `patch_diff` 与 `risk_level`（low/medium/high），高风险改动可要求人工确认。
- 修复和重渲染全过程保留审计轨迹，支持一键回滚到上一版本视频。

### E. 失败与降级策略
- 若无法可靠映射到具体代码段：返回候选问题区间并提示人工介入。
- 若连续渲染失败超过阈值：自动暂停并生成诊断报告，避免无限重试。
- 若视觉回归检查不通过：不覆盖线上版本，只作为候选修复版本。

---

## 9. RAG 升级重点（教育场景）

- 检索过滤维度改为：`education_stage + subject + chapter + textbook_version + difficulty`。
- 引入 `answerability check`：证据不足时明确告知并触发补检索。
- 建立“教学素材库”与“题解素材库”双索引，分别服务文本辅导和视频生成。
- 学科词表从“考研词表”升级为 K12/大学学科词表体系。

---

## 10. 非功能与企业级治理

- **可靠性**：节点级超时预算、工作流补偿、死信队列。
- **安全合规**：未成年人保护策略、内容分级、审计追踪。
- **质量保障**：离线评测集（按学段/学科/角色）+ 回归门禁。
- **成本治理**：模型分层路由（便宜模型优先，复杂题升级高质量模型）。
- **运维可视化**：workflow 成功率、平均时延、工具失败率、视频生成耗时看板。

## 10.1 高并发扩展路线（当前先铺路，未来可直接放量）

### A. 流量分层
- **接入层**：CDN + WAF + API Gateway（统一鉴权、限流、灰度路由）。
- **实时层**：SSE/WebSocket 独立网关服务（Go），与业务 API 解耦。
- **业务层**：无状态业务服务，支持按模块水平扩容。
- **异步层**：视频生成/长耗时任务全部任务化，进入 Kafka/Temporal。

### B. 扩展策略
- 单体模块化 -> 按域拆分微服务（身份、会话、资源、任务）。
- PostgreSQL 读写分离与分库分表预案。
- Redis Cluster + Qdrant 分片副本。
- 热点 API 多级缓存（CDN/网关缓存/Redis）。

### C. 并发能力验收指标（建议目标）
- API 网关：P99 < 300ms（不含 LLM 长耗时）。
- 在线连接：SSE/WebSocket 可扩展到 50k+ 并发连接。
- 聊天入口：峰值 2k~5k RPS 可平滑扩容。
- 视频任务：高峰排队可控，P95 等待时间 < 10s（进入执行队列）。

---

## 11. 重构实施路径（目标态导向）

## Phase 1（2-4 周）：架构契约与流程规范冻结
- 统一 SSE 协议、ID 类型、错误码体系。
- 冻结 SubGraph 规范与消息总线契约（事件 schema、状态分区模型）。
- 建立学段/学科/角色统一枚举与数据字典。

## Phase 2（4-8 周）：Go 业务后端与基础设施底座
- 落地认证、会话、学科、资源、任务中心核心域。
- 接入 Redis、Kafka、PostgreSQL，打通可观测与追踪。

## Phase 3（4-8 周）：Harness 企业化与通信模型切换
- 上线 Workflow DSL、执行状态机、可回放 tracing。
- 将节点通信从共享状态迁移到消息驱动 + 状态分区。
- 上线策略注入、审计链路、灰度发布能力。

## Phase 4（4-8 周）：双引擎与视频修复子图落地
- 将 QA、学习记录、视频生产线纳入统一编排运行时。
- 上线 `VideoRepairSubGraph`（截图诊断、代码修复、重渲染、回归检测）。

## Phase 5（持续）：高并发压测与扩容演练
- 建立容量基线（RPS、在线连接、任务堆积、P99）。
- 按压测结果进行分层扩容与瓶颈拆分。

## Phase 6（持续）：评测与灰度优化
- 学段分层评测集与 A/B 实验。
- 持续优化模型路由与成本效率。

---

## 12. 结论

在你给定的新约束下（**禁用 LangGraph**、**不考虑迁移成本**、**人群聚焦中小学生/大学生/教师**），最优路径是：

- 业务后端（`apps/server`）目标态采用 **Go**，面向未来高并发能力建设。
- Agent 编排坚持自研 Enterprise Harness，不引入 LangGraph。
- 以“个性化辅导 + 个性化教学视频”双引擎重构业务域、Agent 域和数据域。
- 在当前阶段即完成高并发扩展路径铺设（连接层、异步层、缓存层、观测层）。

这条路线面向最终企业级目标态，能够在功能增长和并发增长两个维度同时演进。
